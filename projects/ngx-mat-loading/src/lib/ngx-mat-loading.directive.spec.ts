import { NgxMatLoadingDirective } from './ngx-mat-loading.directive';

describe('NgxMatLoadingDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxMatLoadingDirective();
    expect(directive).toBeTruthy();
  });
});
